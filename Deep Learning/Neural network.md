- what is a neuron?
	- a thing that holds a number 
- the term deep learning refers training of a neural networks or 
- if we consider the ML (linear regression) we have one neuron that takes the input and perform the action on the input value and gives us the output
- and that function is used to call RELU (Rectified linear units)
- every network will take one or other type of input 
# what is neural network 
- so this is a single neuron we get a neural network by stacking up all these types of neurons together
# supervised learning  with neural networks
#### Standard Neural Network 
- we use standard neural network architecture for calculations like 
- feature(X):home features and label(Y):price numerical
- feature(X):what ad is shown and label(Y):click on add numerical
- supervised learning will be done using standard NN 
### Convolution neural network 
- we use it to detect the images 
- and all the supervised learning things with the pixels will be done using the CNNs
### Recurrent neural network 
- we use this to train the machine on sequence pattern 
- like translation and audio 
### Hybrid NN
- for cars and autonomous driving things 
## Structure and Unstructured Data

- are like have a numbers in structure 
- but in unstructured data as it images and audio which will be converted into numbers 
#